[build]
  command = "pnpm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — serve the prerendered shell for any unmatched (client-routed) path.
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
